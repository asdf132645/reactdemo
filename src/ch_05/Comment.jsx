import React from "react";

const styles = {
    wrapper: {
        margin: 8,
        padding: 8,
    }
}

function Comment (props) {
    return (
        <div style={styles.wrapper}>
            <h1>compount</h1>
        </div>
    );
}

export default Comment;